<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta content="@(ViewBag.Description ?? "I like to drink beer when I'm playing video games and when I'm not playing video games.")" name="description">
        <meta content="#171714" name="theme-color">
        <meta content="width=device-width, initial-scale=1.0" name="viewport">
        <title>null-d3v</title>

        <link href="~/img/192.png" rel="icon" type="image/png">
        <link href="~/img/180.png" rel="apple-touch-icon" type="image/png">
        <link href="~/img/32.ico" rel="shortcut icon" type="image/x-icon">

        <link as="style" asp-append-version="true" href="~/css/site.css" rel="preload">
        <link as="script" asp-append-version="true" href="~/js/site.js" rel="preload">

        <link asp-append-version="true" href="~/css/site.css" rel="stylesheet">
    </head>
    <body>
        @await Html.PartialAsync("_Header")

        <main id="content">
            @if (@User.Identity?.IsAuthenticated == true)
            {
                <fieldset class="admin">
                    <legend>Admin</legend>
                    <div>
                        @User.Claims.FirstOrDefault(
                            claim => claim.Type == ClaimTypes.Email)?.Value
                    </div>
                    <div class="admin-action">
                        <a
                            asp-action="Logout"
                            asp-controller="Auth">
                            Log out
                        </a>
                    </div>
                    <div class="admin-action">
                        <a
                            asp-action="Index"
                            asp-controller="Setup">
                            Setup
                        </a>
                    </div>
                    @RenderSection("Admin", required: false)
                </fieldset>
            }
            else
            {
                @RenderSection("Admin", required: false)
            }
            @RenderBody()
        </main>

        <footer id="footer"></footer>

        <script asp-append-version="true" defer src="~/js/site.js"></script>
        @RenderSection("Scripts", required: false)
    </body>
</html>